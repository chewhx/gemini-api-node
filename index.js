const {
  getPastTradesAndConsoleLog,
  getPastTradesAndSave,
} = require("./functions/getPastTrades");
const {
  getPastTransfersAndConsoleLog,
  getPastTransfersAndSave,
} = require("./functions/getTransfers");

/*
 * Make request to get past trades and tabulate results in console
 */

// getPastTradesAndConsoleLog();

/**
 * Make request to get past trades and save results as JSON file in root directory
 */

// getPastTradesAndSave();

/**
 * Make request to get past transfers and tabulate results in console
 */

// getPastTransfersAndConsoleLog();

/**
 * Make request to get past trades and save results as JSON file in root directory
 */

// getPastTransfersAndSave();
